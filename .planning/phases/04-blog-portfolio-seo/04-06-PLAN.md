---
phase: 04-blog-portfolio-seo
plan: 06
type: execute
wave: 4
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified: []
autonomous: false
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - BLOG-04
  - BLOG-05
  - BLOG-06
  - BLOG-07
  - BLOG-08
  - BLOG-09
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
must_haves:
  truths:
    - "Blog listing shows paginated card grid at 6 posts per page"
    - "Blog detail renders rich content with reading time, author card, share buttons, related posts"
    - "Category and tag archive pages filter posts correctly"
    - "Portfolio gallery shows filterable grid with hover overlays"
    - "Case study detail shows results metrics, before/after images"
    - "RSS feed and XML sitemap return valid XML"
    - "All pages have OG tags, canonical URLs, hreflang in server-rendered HTML"
    - "JSON-LD schemas validate without errors"
    - "Admin can manage categories, SEO settings, and assign categories/tags to posts"
  artifacts: []
  key_links: []
---

<objective>
Full verification checkpoint for Phase 4. Verify all blog, portfolio, and SEO features work end-to-end with seed data.

Purpose: Ensure all 18 requirements (BLOG-01 through BLOG-09, PORT-01 through PORT-04, SEO-01 through SEO-05) are met before marking the phase complete.

Output: Verification results confirming all features work.
</objective>

<execution_context>
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-blog-portfolio-seo/04-CONTEXT.md
@.planning/phases/04-blog-portfolio-seo/04-01-SUMMARY.md
@.planning/phases/04-blog-portfolio-seo/04-02-SUMMARY.md
@.planning/phases/04-blog-portfolio-seo/04-03-SUMMARY.md
@.planning/phases/04-blog-portfolio-seo/04-04-SUMMARY.md
@.planning/phases/04-blog-portfolio-seo/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed test data and run automated verification checks</name>
  <files></files>
  <read_first>
    database/seeders/DatabaseSeeder.php,
    app/Models/BlogPost.php,
    app/Models/Category.php,
    app/Models/Tag.php,
    app/Models/PortfolioItem.php,
    app/Models/SeoMetadata.php
  </read_first>
  <action>
    Create seed data for verification if not already present. Use `php artisan tinker` to create:

    1. **Categories** (4 matching service types + 1 general per D-05):
       - Development (slug: development, sort_order: 1)
       - Automation (slug: automation, sort_order: 2)
       - QA (slug: qa, sort_order: 3)
       - Cybersecurity (slug: cybersecurity, sort_order: 4)
       - General (slug: general, sort_order: 5)

    2. **Tags** (3-4 sample tags):
       - Laravel, React, TypeScript, Security

    3. **Blog posts** (at least 7 published posts to test pagination at 6/page):
       - Assign categories and tags to posts
       - Ensure at least 2 posts share a category (for related posts testing)
       - Ensure at least 1 post has SEO meta fields filled

    4. **Portfolio items** (at least 3 published items with different service categories):
       - Include results_metrics JSON on at least 1 item
       - Ensure items cover at least 2 different service categories for filter testing

    5. **SEO metadata** for at least one static page (e.g., home)

    Then run automated checks:

    **Route checks:**
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/en/blog` = 200
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/en/portfolio` = 200
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/en/feed.xml` = 200
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/sitemap.xml` = 200

    **Content checks:**
    - `curl -s http://localhost:8000/sitemap.xml | grep -c '<url>'` returns > 20 (10 static pages * 2 locales + blog + portfolio)
    - `curl -s http://localhost:8000/en/feed.xml | grep -c '<item>'` returns > 0
    - `curl -s http://localhost:8000/en/blog | grep -c 'og:title'` returns > 0 (server-side OG tag)
    - `curl -s http://localhost:8000/en | grep -c 'hreflang'` returns >= 2 (en + ar)
    - `curl -s http://localhost:8000/en | grep -c 'canonical'` returns > 0
    - `curl -s http://localhost:8000/en | grep -c 'schema.org'` returns > 0 (Organization JSON-LD)

    **Admin route checks:**
    - `php artisan route:list --path=admin/categories` shows routes
    - `php artisan route:list --path=admin/seo` shows routes

    If the dev server is not running, start it with `php artisan serve` in background first. Use `--port=8000`.
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && php artisan route:list --columns=method,uri --path=blog 2>&1 | head -20 && php artisan route:list --columns=method,uri --path=portfolio 2>&1 | head -10 && php artisan route:list --columns=method,uri --path=sitemap 2>&1 | head -5 && php artisan route:list --columns=method,uri --path=feed 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All blog routes registered (blog.index, blog.show, blog.category, blog.tag, blog.author)
    - All portfolio routes registered (portfolio.index, portfolio.show)
    - Sitemap route registered at /sitemap.xml
    - Feed route registered at /{locale}/feed.xml
    - Admin category and SEO routes registered
    - Seed data exists: at least 7 published blog posts, at least 3 published portfolio items
    - At least 5 blog categories exist
    - At least 3 blog tags exist
  </acceptance_criteria>
  <done>Seed data created. All routes verified. Automated content checks pass for OG tags, hreflang, canonical URLs, JSON-LD, sitemap, and RSS feed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all Phase 4 features</name>
  <files></files>
  <action>
    Present the user with a checklist of all Phase 4 features to verify visually and functionally. The automated checks from Task 1 confirm routes and data exist. This task confirms the visual quality and interactive behavior.

    Features built in Phase 4:

    **Blog:**
    - Blog listing at /en/blog with card grid (2 columns desktop, 1 mobile), category filter tabs, pagination (6/page)
    - Blog post detail with rich content (prose typography), reading time, author profile card, share buttons (LinkedIn, X, WhatsApp), related posts
    - Category archive pages (/en/blog/category/{slug})
    - Tag archive pages (/en/blog/tag/{slug})
    - Author archive pages (/en/blog/author/{id})

    **Portfolio:**
    - Portfolio gallery at /en/portfolio with 3-column grid, hover overlays, service type filter tabs
    - Case study detail with narrative content, animated result metrics, before/after images

    **SEO:**
    - RSS feed at /en/feed.xml (valid RSS 2.0)
    - XML sitemap at /sitemap.xml (all pages, both languages)
    - OG tags, canonical URLs, hreflang on every page (view source to verify)
    - JSON-LD: Organization (home), Service (service pages), Article (blog), LocalBusiness (contact), FAQ (FAQ page)

    **Admin:**
    - Category management page
    - Blog post forms with category/tag assignment and SEO fields
    - Portfolio forms with before/after images and SEO fields
    - Static page SEO settings

    Verification steps:
    1. Visit http://localhost:8000/en/blog -- verify card grid with images, titles, excerpts, author, date, reading time, category badges. Verify pagination works (page 2 if > 6 posts). Click a category tab to filter.
    2. Click a blog post -- verify rich content renders with proper typography, reading time near title, share buttons present, author card below content with bio and social links, related posts at bottom.
    3. Visit http://localhost:8000/ar/blog -- verify RTL layout, Arabic content, RTL text alignment.
    4. Visit http://localhost:8000/en/portfolio -- verify 3-column grid with hover overlays showing project info. Click a filter tab. Verify fade animation on filter change.
    5. Click a portfolio item -- verify case study content, result metrics with animated counters, before/after images (if populated).
    6. Visit http://localhost:8000/sitemap.xml -- verify valid XML with all page URLs.
    7. Visit http://localhost:8000/en/feed.xml -- verify valid RSS with blog post items.
    8. View page source of http://localhost:8000/en -- verify og:title, og:description, canonical, hreflang (en + ar), and Organization JSON-LD in the HTML.
    9. Visit /admin/categories -- verify category management works (create, edit, delete).
    10. Visit /admin/blog/create -- verify category checkboxes, tag input, and SEO fields section.
  </action>
  <verify>User confirms all features work correctly or reports issues.</verify>
  <acceptance_criteria>
    - User confirms blog listing renders correctly with card grid and pagination
    - User confirms blog detail shows rich content, reading time, author card, share buttons, related posts
    - User confirms portfolio gallery works with filter and hover overlays
    - User confirms SEO meta tags visible in page source
    - User confirms admin extensions work (categories, SEO fields, SEO settings)
  </acceptance_criteria>
  <done>User approves all Phase 4 features. Phase 4 is complete.</done>
</task>

</tasks>

<verification>
All 18 requirements verified through automated route checks and manual visual inspection.
</verification>

<success_criteria>
All Phase 4 success criteria from ROADMAP.md are met:
1. Blog listing with pagination, category/tag filtering
2. Blog detail with rich content, reading time, author profile, related posts, share buttons, RSS feed
3. Portfolio gallery with hover previews and service type filtering; case study detail with metrics and before/after
4. Admin-editable meta titles/descriptions, OG tags, canonical URLs, hreflang on all pages, XML sitemap
5. JSON-LD structured data validates on relevant pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-blog-portfolio-seo/04-06-SUMMARY.md`
</output>
