---
phase: 05-brand-design-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - resources/js/components/ui/button.tsx
  - resources/js/components/ui/card.tsx
  - resources/js/components/ui/input.tsx
  - resources/js/components/ui/badge.tsx
autonomous: true
requirements:
  - BRAND-03

must_haves:
  truths:
    - "Public-facing cards use glass-morphism in dark mode (backdrop-blur + translucent bg) and clean white with soft shadow in light mode"
    - "Cards have a hover state with subtle lift (translateY -2px) and shadow increase"
    - "Buttons have three distinct brand variants: solid primary (teal bg), outline (teal border), and ghost (no border, teal on hover)"
    - "All buttons show hover brightness shift + subtle scale(1.02) and active press scale(0.98)"
    - "Inputs use the new border radius and focus ring that matches primary color"
    - "All components use rounded-lg (8px base radius) not rounded-xl (16px)"
  artifacts:
    - path: "resources/js/components/ui/card.tsx"
      provides: "Glass-morphism card with hover lift"
      contains: "backdrop-blur"
    - path: "resources/js/components/ui/button.tsx"
      provides: "Brand button variants with interaction animations"
      contains: "scale-[1.02]"
    - path: "resources/js/components/ui/input.tsx"
      provides: "Updated input with brand focus ring"
      contains: "focus-visible:ring-ring/50"
  key_links:
    - from: "resources/js/components/ui/card.tsx"
      to: "resources/css/app.css"
      via: "CSS custom properties (--card, --border, --primary)"
      pattern: "bg-card.*dark:bg-card/80.*dark:backdrop-blur"
    - from: "resources/js/components/ui/button.tsx"
      to: "resources/css/app.css"
      via: "CSS custom properties (--primary, --ring)"
      pattern: "bg-primary.*hover:bg-primary/90.*scale"
---

<objective>
Update the core UI component styling (cards, buttons, inputs, badges) to implement the new brand design language -- glass-morphism dark mode cards, interaction animations on buttons, and consistent border-radius and focus rings.

Purpose: These components are used across all public pages. Updating them propagates the new brand feel everywhere without touching individual page files.
Output: Updated card.tsx, button.tsx, input.tsx, badge.tsx with new brand styling.
</objective>

<execution_context>
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-brand-design-system/05-CONTEXT.md
@.planning/phases/05-brand-design-system/05-RESEARCH.md
@.planning/phases/05-brand-design-system/05-01-SUMMARY.md

<interfaces>
<!-- Current component structures -- executor updates className strings, preserves component API -->
From resources/js/components/ui/card.tsx:
```tsx
function Card({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card"
      className={cn(
        "bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm hover:shadow-lg hover:shadow-primary/5 hover:-translate-y-0.5 transition-all duration-300",
        className
      )}
      {...props}
    />
  )
}
// Also: CardHeader, CardTitle, CardDescription, CardContent, CardFooter
```

From resources/js/components/ui/button.tsx:
```tsx
const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all duration-200 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground shadow-xs hover:bg-primary/90 hover:shadow-md hover:shadow-primary/25 hover:-translate-y-0.5 transition-all duration-200",
        destructive: "bg-destructive text-white shadow-xs hover:bg-destructive/90 ...",
        outline: "border border-input bg-background shadow-xs hover:bg-accent/10 hover:text-accent-foreground hover:border-primary/50 transition-all duration-200",
        secondary: "bg-secondary text-secondary-foreground shadow-xs hover:bg-secondary/80 hover:-translate-y-0.5 transition-all duration-200",
        ghost: "hover:bg-accent/10 hover:text-accent-foreground transition-all duration-200",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: { default: "h-9 px-4 py-2 has-[>svg]:px-3", sm: "...", lg: "...", icon: "..." },
    },
  }
)
```

From resources/js/components/ui/input.tsx:
```tsx
className={cn(
  "border-input file:text-foreground placeholder:text-muted-foreground selection:bg-primary selection:text-primary-foreground flex h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-all duration-200 outline-none ...",
  "focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]",
  "aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
  className
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Card component with glass-morphism and brand styling</name>
  <files>resources/js/components/ui/card.tsx</files>
  <read_first>
    - resources/js/components/ui/card.tsx (current full file -- preserve all sub-components and exports)
    - .planning/phases/05-brand-design-system/05-CONTEXT.md (decision D-13: cards specification)
  </read_first>
  <action>
Update the Card component className in `resources/js/components/ui/card.tsx` per decision D-13.

**Card function** -- replace the entire className string with:
```
"bg-card text-card-foreground flex flex-col gap-6 rounded-lg border border-border/50 py-6 shadow-sm transition-all duration-300 hover:shadow-md hover:-translate-y-0.5 dark:bg-card/80 dark:backdrop-blur-xl dark:border-border/30 dark:hover:border-primary/20 dark:hover:shadow-primary/5"
```

Key changes from current:
- `rounded-xl` -> `rounded-lg` (matches 8px base radius per D-10)
- Added `border-border/50` (50% opacity border per D-13)
- `hover:shadow-lg` -> `hover:shadow-md` (less aggressive hover shadow)
- Removed `hover:shadow-primary/5` from light mode (keep subtle)
- Added dark mode glass-morphism: `dark:bg-card/80 dark:backdrop-blur-xl dark:border-border/30`
- Added dark mode hover glow: `dark:hover:border-primary/20 dark:hover:shadow-primary/5`

Do NOT change CardHeader, CardTitle, CardDescription, CardContent, CardFooter, or any exports. Only update the Card function's className.
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && grep "backdrop-blur" resources/js/components/ui/card.tsx && grep "rounded-lg" resources/js/components/ui/card.tsx && grep "border-border/50" resources/js/components/ui/card.tsx && grep "dark:bg-card/80" resources/js/components/ui/card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "backdrop-blur-xl" resources/js/components/ui/card.tsx` returns a match (glass-morphism in dark mode)
    - `grep "rounded-lg" resources/js/components/ui/card.tsx` returns a match (not rounded-xl)
    - `grep -c "rounded-xl" resources/js/components/ui/card.tsx` returns 0 (old radius removed)
    - `grep "dark:bg-card/80" resources/js/components/ui/card.tsx` returns a match (translucent dark background)
    - `grep "dark:border-border/30" resources/js/components/ui/card.tsx` returns a match (subtle dark border)
    - `grep "dark:hover:border-primary/20" resources/js/components/ui/card.tsx` returns a match (hover glow)
    - `grep "border-border/50" resources/js/components/ui/card.tsx` returns a match (light mode border opacity)
    - `grep "hover:shadow-md" resources/js/components/ui/card.tsx` returns a match (hover shadow increase)
    - `grep "hover:-translate-y-0.5" resources/js/components/ui/card.tsx` returns a match (hover lift preserved)
    - All 6 exports preserved: Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent
  </acceptance_criteria>
  <done>Card component uses glass-morphism (backdrop-blur + translucent bg) in dark mode, clean border/shadow in light mode, rounded-lg radius, and hover lift with shadow increase.</done>
</task>

<task type="auto">
  <name>Task 2: Update Button, Input, and Badge components with brand styling</name>
  <files>resources/js/components/ui/button.tsx, resources/js/components/ui/input.tsx, resources/js/components/ui/badge.tsx</files>
  <read_first>
    - resources/js/components/ui/button.tsx (current CVA variants -- preserve component API and size variants)
    - resources/js/components/ui/input.tsx (current className -- preserve component API)
    - resources/js/components/ui/badge.tsx (current variants -- preserve component API)
    - .planning/phases/05-brand-design-system/05-CONTEXT.md (decisions D-14, D-15)
  </read_first>
  <action>
**Button (resources/js/components/ui/button.tsx) -- per D-14:**

Update the CVA `buttonVariants` variant strings. Keep the base string and size variants unchanged. Only update variant class strings:

Replace `default` variant with:
```
"bg-primary text-primary-foreground shadow-xs hover:bg-primary/90 hover:shadow-md hover:shadow-primary/25 hover:-translate-y-0.5 hover:scale-[1.02] active:scale-[0.98] transition-all duration-200"
```

Replace `outline` variant with:
```
"border border-primary/50 bg-transparent text-primary shadow-xs hover:bg-primary/5 hover:border-primary hover:scale-[1.02] active:scale-[0.98] transition-all duration-200"
```

Replace `ghost` variant with:
```
"hover:bg-primary/5 hover:text-primary hover:scale-[1.02] active:scale-[0.98] transition-all duration-200"
```

Replace `secondary` variant with:
```
"bg-secondary text-secondary-foreground shadow-xs hover:bg-secondary/80 hover:-translate-y-0.5 hover:scale-[1.02] active:scale-[0.98] transition-all duration-200"
```

Keep `destructive` and `link` variants unchanged.

Key changes:
- Added `hover:scale-[1.02]` and `active:scale-[0.98]` to default, outline, ghost, secondary (interaction animation per D-18)
- `outline` variant: `border-input` -> `border-primary/50`, `bg-background` -> `bg-transparent`, `text-primary` added, hover uses `bg-primary/5` and `hover:border-primary`
- `ghost` variant: hover color changed from `accent/10` to `primary/5`, text from `accent-foreground` to `primary`

**Input (resources/js/components/ui/input.tsx) -- per D-15:**

The input component is already close to the target. Update the className to ensure:
- `rounded-md` stays (matches 6px = radius-md, appropriate for inputs)
- Focus ring already uses `focus-visible:ring-ring/50` which matches primary color via --ring token
- No changes needed to the focus ring or border-radius -- they already match the brand spec

The only change: add `dark:bg-card/50` for a subtle surface tint in dark mode inputs (rather than fully transparent):

Replace the first line of the className:
```
"border-input file:text-foreground placeholder:text-muted-foreground selection:bg-primary selection:text-primary-foreground flex h-9 w-full min-w-0 rounded-md border bg-transparent dark:bg-card/50 px-3 py-1 text-base shadow-xs transition-all duration-200 outline-none file:inline-flex file:h-7 file:border-0 file:bg-transparent file:text-sm file:font-medium disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50 md:text-sm"
```

Key change: Added `dark:bg-card/50` after `bg-transparent` for subtle dark mode surface tint.

**Badge (resources/js/components/ui/badge.tsx):**

Read the file first to see current structure. Update:
- Change any `rounded-full` to `rounded-md` to match the more angular brand aesthetic (badges should feel like part of the same design system, not pill-shaped when other elements are rectangular)
- If the badge has a `default` variant using a generic color, update it to use `bg-primary/10 text-primary` for brand alignment
- Keep all other variants and the component API unchanged

Note: If badge already uses `rounded-md`, no radius change needed. Only adjust if it uses `rounded-full`.
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && grep "scale-\[1.02\]" resources/js/components/ui/button.tsx | wc -l && grep "active:scale-\[0.98\]" resources/js/components/ui/button.tsx | wc -l && grep "border-primary/50" resources/js/components/ui/button.tsx && grep "dark:bg-card/50" resources/js/components/ui/input.tsx && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scale-\[1.02\]" resources/js/components/ui/button.tsx` returns 4 (default, outline, ghost, secondary variants)
    - `grep -c "active:scale-\[0.98\]" resources/js/components/ui/button.tsx` returns 4 (same four variants)
    - `grep "border-primary/50" resources/js/components/ui/button.tsx` returns the outline variant line
    - `grep "bg-transparent text-primary" resources/js/components/ui/button.tsx` returns the outline variant
    - `grep "hover:bg-primary/5 hover:text-primary" resources/js/components/ui/button.tsx` returns the ghost variant
    - `grep "dark:bg-card/50" resources/js/components/ui/input.tsx` returns a match
    - `grep "rounded-md" resources/js/components/ui/input.tsx` returns a match (consistent radius)
    - Destructive and link button variants are unchanged
    - `npx vite build` completes without errors
    - Button and Input exports are preserved (Button, buttonVariants, Input)
  </acceptance_criteria>
  <done>Button variants updated with scale interaction animations (1.02 hover, 0.98 active), outline uses teal border/text, ghost uses primary color. Input has dark mode surface tint. Badge radius aligned with design system. Vite build passes.</done>
</task>

</tasks>

<verification>
- `grep "backdrop-blur" resources/js/components/ui/card.tsx` confirms glass-morphism
- `grep "scale-\[1.02\]" resources/js/components/ui/button.tsx` confirms interaction animations
- `grep "dark:bg-card/50" resources/js/components/ui/input.tsx` confirms dark input styling
- `npx vite build` completes without errors
- All component exports unchanged (no API breaks)
</verification>

<success_criteria>
- BRAND-03: Cards use glass-morphism dark mode, soft shadow light mode, rounded-lg radius, hover lift
- BRAND-03: Buttons have solid/outline/ghost brand variants with scale interaction animations
- BRAND-03: Inputs use consistent radius and primary-colored focus ring with dark surface tint
- All component function signatures and exports unchanged
- Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-brand-design-system/05-02-SUMMARY.md`
</output>
