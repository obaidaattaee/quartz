---
phase: 02-public-marketing-site
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - resources/js/components/hero-section.tsx
  - resources/js/components/service-card.tsx
  - resources/js/components/client-logos.tsx
  - resources/js/components/statistics-section.tsx
  - resources/js/components/testimonial-card.tsx
  - resources/js/components/comparison-section.tsx
  - resources/js/components/cta-section.tsx
  - resources/js/hooks/use-counter.ts
  - resources/js/pages/public/home.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07]
user_setup: []

must_haves:
  truths:
    - "Landing page displays animated hero with value proposition and CTA button"
    - "4 service cards link to their respective detail pages"
    - "Client logo bar shows grayscale logos that colorize on hover"
    - "Statistics counters animate upward when scrolled into view"
    - "3 testimonial cards show quotes with client names and companies"
    - "Old Way vs New Way comparison displays in two columns"
    - "Bottom CTA section links to the contact page"
    - "All sections use ScrollReveal animations"
    - "All text renders from translation keys (no hardcoded strings)"
  artifacts:
    - path: "resources/js/components/hero-section.tsx"
      provides: "Hero with gradient background, floating shapes, staggered text animation"
      contains: "export default function HeroSection"
    - path: "resources/js/components/service-card.tsx"
      provides: "Service overview card with icon, title, description, hover effect"
      contains: "export default function ServiceCard"
    - path: "resources/js/hooks/use-counter.ts"
      provides: "Scroll-triggered count-up animation hook using requestAnimationFrame"
      contains: "export function useCounter"
    - path: "resources/js/components/statistics-section.tsx"
      provides: "4 animated stat counters"
      contains: "useCounter"
    - path: "resources/js/pages/public/home.tsx"
      provides: "Full landing page assembling all sections in order per D-15"
      min_lines: 40
  key_links:
    - from: "resources/js/pages/public/home.tsx"
      to: "hero-section.tsx"
      via: "import HeroSection"
      pattern: "import HeroSection"
    - from: "resources/js/pages/public/home.tsx"
      to: "statistics-section.tsx"
      via: "import StatisticsSection"
      pattern: "import StatisticsSection"
    - from: "resources/js/components/service-card.tsx"
      to: "/{locale}/services/{slug}"
      via: "Inertia Link component"
      pattern: "Link.*href"
    - from: "resources/js/components/statistics-section.tsx"
      to: "use-counter.ts"
      via: "import { useCounter }"
      pattern: "import.*useCounter"
---

<objective>
Build the complete landing page with all 7 sections: hero, service overview cards, client logos, statistics counters, testimonials, Old Way vs New Way comparison, and bottom CTA.

Purpose: The landing page is the primary entry point for all visitors. It must immediately convey what Quartz does, show proof of quality, and drive visitors to contact or service detail pages.

Output: 7 new components, 1 custom hook, and the fully assembled home page replacing the current placeholder.
</objective>

<execution_context>
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-marketing-site/02-CONTEXT.md
@.planning/phases/02-public-marketing-site/02-RESEARCH.md
@.planning/phases/02-public-marketing-site/02-01-SUMMARY.md

<interfaces>
<!-- Phase 01 animation system and layout components the executor must use -->

From resources/js/lib/animations.ts:
```typescript
export const fadeInUp: Variants;        // opacity 0->1, y 20->0
export const fadeIn: Variants;          // opacity 0->1
export const scaleIn: Variants;         // opacity 0->1, scale 0.95->1
export const staggerContainer: Variants; // staggerChildren: 0.1
export const staggerContainerFast: Variants; // staggerChildren: 0.05
export const heroEntrance: Variants;    // opacity 0->1, y 30->0
export const defaultTransition;         // duration: 0.4
export const heroTransition;            // duration: 0.6
export const quickTransition;           // duration: 0.2
```

From resources/js/components/scroll-reveal.tsx:
```typescript
type Props = {
    children: ReactNode;
    delay?: number;
    className?: string;
    variant?: 'default' | 'hero' | 'stagger';
    as?: 'div' | 'section' | 'article' | 'ul';
};
export default function ScrollReveal(props: Props);
```

From resources/js/hooks/use-locale.tsx:
```typescript
export function useLocale(): { locale: string; direction: string; isRTL: boolean; t: (key: string) => string; switchLocaleUrl: string };
```

From resources/js/hooks/use-reduced-motion.tsx:
```typescript
export function useReducedMotion(): boolean;
```

From resources/js/components/ui/card.tsx:
```typescript
// Exports: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
// Card has hover micro-interaction: hover:-translate-y-0.5 transition-all
```

From resources/js/components/ui/button.tsx:
```typescript
// Exports: Button with variants: default, destructive, outline, secondary, ghost, link
// Sizes: default, sm, lg, icon
```

From resources/js/layouts/public-layout.tsx:
```typescript
export default function PublicLayout({ children, breadcrumbs }: { children: ReactNode; breadcrumbs?: BreadcrumbItem[] });
```

Page pattern (from existing home.tsx):
```typescript
export default function Home() { /* ... */ }
Home.layout = (page: React.ReactNode) => (
    <PublicLayout>{page}</PublicLayout>
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section, statistics counter hook, CTA section, and service card components</name>
  <files>
    resources/js/components/hero-section.tsx,
    resources/js/hooks/use-counter.ts,
    resources/js/components/statistics-section.tsx,
    resources/js/components/cta-section.tsx,
    resources/js/components/service-card.tsx
  </files>
  <read_first>
    resources/js/components/scroll-reveal.tsx,
    resources/js/lib/animations.ts,
    resources/js/hooks/use-locale.tsx,
    resources/js/hooks/use-reduced-motion.tsx,
    resources/js/components/ui/card.tsx,
    resources/js/components/ui/button.tsx,
    resources/css/app.css,
    resources/lang/en.json
  </read_first>
  <action>
    **1. Create `resources/js/components/hero-section.tsx`** (per D-01, D-02, D-03, LAND-01):
    - Full-viewport-height section (`min-h-screen`) with gradient background
    - Gradient: `bg-gradient-to-br from-background via-background to-primary/10 dark:from-background dark:via-background dark:to-primary/20` (adapts to light/dark per D-03)
    - Floating geometric shapes: 3-5 `<motion.div>` elements with subtle rotate/float animations, positioned absolutely. Use teal/primary color with low opacity (5-10%). Shapes: circles and rounded rectangles using CSS (`rounded-full`, `rounded-2xl`). Only render if `!shouldReduceMotion`.
    - Content container: `max-w-7xl mx-auto px-4 pt-32 pb-20 relative z-10`
    - Use `motion.div` with `staggerContainer` variants (initial="hidden" animate="visible")
    - `motion.h1` with `heroEntrance` variant + `heroTransition`: render `t('hero.title')` with `text-4xl font-bold tracking-tight md:text-6xl lg:text-7xl`
    - `motion.p` with `fadeInUp` variant + delay 0.2: render `t('hero.subtitle')` with `text-lg md:text-xl text-muted-foreground mt-6 max-w-2xl`
    - `motion.div` with `fadeInUp` variant + delay 0.4: render Button component (`<Button size="lg">`) linking to `/${locale}/contact` with text `t('hero.cta')`. Style: add `shadow-lg shadow-primary/25 hover:shadow-primary/40` for glow effect per D-02.
    - Import `Link` from `@inertiajs/react` for the CTA button href.
    - Check `shouldReduceMotion` from `useReducedMotion()` -- if true, set `initial={false}` on all motion elements.

    **2. Create `resources/js/hooks/use-counter.ts`** (per D-16, LAND-05):
    - Custom hook using `motion/react`'s `useInView` + `requestAnimationFrame`
    - Signature: `export function useCounter(target: number, duration?: number): { ref: RefObject<HTMLDivElement | null>; count: number }`
    - Default duration: 2000ms
    - Use `useInView(ref, { once: true, amount: 0.5 })` to trigger only when 50% visible
    - Animate from 0 to target using `requestAnimationFrame` loop with ease-out-quad easing: `const eased = 1 - (1 - progress) * (1 - progress)`
    - Set count via `useState` during animation, final value exactly equals `target`
    - If `useReducedMotion()` returns true, skip animation and set count to target immediately
    - Implementation per 02-RESEARCH.md "Pattern 3" code example

    **3. Create `resources/js/components/statistics-section.tsx`** (per D-16, LAND-05):
    - Section with `max-w-7xl mx-auto` container
    - Title: `t('stats.title')` in ScrollReveal
    - 4 stat items in a responsive grid: `grid grid-cols-2 md:grid-cols-4 gap-8`
    - Each stat uses the `useCounter` hook with target from `parseInt(t('stats.{key}.value'))`
    - Display: large number (`text-4xl md:text-5xl font-bold text-primary`) + suffix (e.g., "+") + label below
    - Stat keys: `years`, `projects`, `clients`, `team`
    - Each stat item: `<div ref={ref}><span>{count}{t('stats.{key}.suffix')}</span><span>{t('stats.{key}.label')}</span></div>`
    - Wrap the entire section in `<ScrollReveal as="section">`

    **4. Create `resources/js/components/cta-section.tsx`** (per LAND-07):
    - Full-width section with gradient background similar to hero but less intense
    - Background: `bg-primary/5 dark:bg-primary/10` or similar subtle teal wash
    - Centered content with `text-center max-w-2xl mx-auto`
    - Title: `t('cta.title')` -- large heading
    - Subtitle: `t('cta.subtitle')` -- muted description
    - Button: `<Button size="lg" asChild><Link href={'/${locale}/contact'}>{t('cta.button')}</Link></Button>`
    - Wrap in `<ScrollReveal as="section">`

    **5. Create `resources/js/components/service-card.tsx`** (per D-06, LAND-02):
    - Props: `{ icon: LucideIcon; titleKey: string; briefKey: string; href: string }`
    - Uses Card component from `@/components/ui/card` (which has built-in hover micro-interaction)
    - Renders: icon (size-10, text-primary), title from `t(titleKey)`, brief from `t(briefKey)`
    - Wraps Card in `<Link href={href}>` to make entire card clickable (Inertia Link)
    - Add `<motion.div variants={fadeInUp}>` wrapper for stagger animation when used inside stagger container
    - Footer with "Learn More" text + arrow icon: `t('services.learnMore')` with `ArrowRight` (or `ArrowLeft` if `isRTL`) from lucide-react
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && test -f resources/js/components/hero-section.tsx && test -f resources/js/hooks/use-counter.ts && test -f resources/js/components/statistics-section.tsx && test -f resources/js/components/cta-section.tsx && test -f resources/js/components/service-card.tsx && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "All 5 files exist"</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/components/hero-section.tsx contains `export default function HeroSection`
    - hero-section.tsx contains `t('hero.title')` and `t('hero.subtitle')` and `t('hero.cta')`
    - hero-section.tsx contains `staggerContainer` import from `@/lib/animations`
    - hero-section.tsx contains `heroEntrance` import from `@/lib/animations`
    - hero-section.tsx contains `useReducedMotion` check
    - hero-section.tsx contains gradient classes with `dark:` variants for dual-mode support
    - hero-section.tsx contains `Link` from `@inertiajs/react` for CTA navigation
    - resources/js/hooks/use-counter.ts contains `export function useCounter`
    - use-counter.ts contains `useInView` import from `motion/react`
    - use-counter.ts contains `requestAnimationFrame`
    - use-counter.ts contains `once: true, amount: 0.5` in useInView options
    - resources/js/components/statistics-section.tsx contains `useCounter` import
    - statistics-section.tsx contains `t('stats.years.value')` or equivalent stat key access
    - resources/js/components/cta-section.tsx contains `t('cta.title')`
    - cta-section.tsx contains Link to contact page
    - resources/js/components/service-card.tsx contains `export default function ServiceCard`
    - service-card.tsx contains `Link` import from `@inertiajs/react`
    - service-card.tsx contains `Card` import from `@/components/ui/card`
    - npx tsc --noEmit produces no errors in these files
  </acceptance_criteria>
  <done>Hero section, statistics counter hook, statistics section, CTA section, and service card components all created with animations, translations, and proper TypeScript types.</done>
</task>

<task type="auto">
  <name>Task 2: Client logos, testimonials, comparison section, and full landing page assembly</name>
  <files>
    resources/js/components/client-logos.tsx,
    resources/js/components/testimonial-card.tsx,
    resources/js/components/comparison-section.tsx,
    resources/js/pages/public/home.tsx
  </files>
  <read_first>
    resources/js/components/hero-section.tsx,
    resources/js/components/service-card.tsx,
    resources/js/components/statistics-section.tsx,
    resources/js/components/cta-section.tsx,
    resources/js/components/scroll-reveal.tsx,
    resources/js/lib/animations.ts,
    resources/js/hooks/use-locale.tsx,
    resources/js/components/ui/card.tsx,
    resources/js/layouts/public-layout.tsx,
    resources/js/pages/public/home.tsx,
    resources/lang/en.json
  </read_first>
  <action>
    **1. Create `resources/js/components/client-logos.tsx`** (per D-19, LAND-03):
    - Section with title `t('clients.title')` in a ScrollReveal
    - Horizontal flex row of 6-8 placeholder logo elements: `flex flex-wrap items-center justify-center gap-8 md:gap-12`
    - Each "logo" is a placeholder rounded rectangle with company name text inside (since real logos come via admin in Phase 3): `<div className="grayscale opacity-60 transition-all duration-300 hover:grayscale-0 hover:opacity-100">` containing a styled box with a fake company name like "TechCorp", "GlobalTrade", "FinSecure", "DataFlow", "CloudNine", "SecureNet"
    - Use SVG rectangles or styled divs (h-12 w-28 rounded-lg bg-muted flex items-center justify-center) as placeholder logo shapes
    - Logos grayscale by default, colorize on hover per D-19
    - Wrap in `<ScrollReveal as="section">`

    **2. Create `resources/js/components/testimonial-card.tsx`** (per D-17, LAND-04):
    - Props: `{ index: number }` -- reads content from translation keys: `t('testimonials.{index}.quote')`, `t('testimonials.{index}.name')`, etc.
    - Uses Card component from `@/components/ui/card`
    - Layout: quote text (italic, text-lg), then bottom section with avatar placeholder (div with initials), name (font-semibold), role, and company
    - Quote icon: use a large quotation mark character or Lucide `Quote` icon at top of card, text-primary/20
    - Wrap in `<motion.div variants={fadeInUp}>` for stagger animation

    **3. Create `resources/js/components/comparison-section.tsx`** (per D-18, LAND-06):
    - Section title: `t('comparison.title')` in ScrollReveal
    - Two-column grid: `grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8`
    - Left card ("Old Way"): Card with red/destructive accent. Title: `t('comparison.oldWay.title')`. List 5 pain points from `t('comparison.oldWay.0')` through `t('comparison.oldWay.4')`, each with an X icon (`X` from lucide-react, `text-destructive`)
    - Right card ("New Way"): Card with teal/primary accent. Title: `t('comparison.newWay.title')`. List 5 benefits from `t('comparison.newWay.0')` through `t('comparison.newWay.4')`, each with a check icon (`Check` from lucide-react, `text-primary`)
    - Both cards wrapped in ScrollReveal with stagger delay (left: 0, right: 0.15)
    - Works in RTL via CSS logical properties (grid auto-mirrors)

    **4. Replace `resources/js/pages/public/home.tsx`** with full landing page (per D-15):
    - Import all 7 section components
    - Section order per D-15: HeroSection -> Service Overview -> Client Logos -> Statistics -> Testimonials -> Comparison -> CTA
    - **Service Overview section** (inline in home.tsx, not a separate component):
      - Section title: `t('services.overview.title')` and subtitle `t('services.overview.subtitle')` in ScrollReveal
      - Grid of 4 ServiceCard components: `<ScrollReveal variant="stagger" as="div" className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">`
      - Card 1: icon=`Code2` (from lucide-react), titleKey="services.development.title", briefKey="services.development.brief", href=`/${locale}/services/development`
      - Card 2: icon=`Zap`, titleKey="services.automation.title", briefKey="services.automation.brief", href=`/${locale}/services/automation`
      - Card 3: icon=`ShieldCheck`, titleKey="services.qa.title", briefKey="services.qa.brief", href=`/${locale}/services/qa`
      - Card 4: icon=`Lock`, titleKey="services.cybersecurity.title", briefKey="services.cybersecurity.brief", href=`/${locale}/services/cybersecurity`
    - **Testimonials section** (inline in home.tsx):
      - Section title: `t('testimonials.title')` in ScrollReveal
      - `<ScrollReveal variant="stagger" as="div" className="grid grid-cols-1 md:grid-cols-3 gap-6">` containing 3 TestimonialCard components (index 0, 1, 2)
    - Each major section gets vertical padding: `py-16 md:py-24`
    - Each section uses `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` for consistent width
    - Head title: `t('nav.home')` (existing key)
    - Use PublicLayout via `.layout` property (no breadcrumbs on home page)
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && test -f resources/js/components/client-logos.tsx && test -f resources/js/components/testimonial-card.tsx && test -f resources/js/components/comparison-section.tsx && test -f resources/js/pages/public/home.tsx && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "All 4 files exist and checked"</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/components/client-logos.tsx contains `export default function ClientLogos`
    - client-logos.tsx contains `grayscale` class for hover effect
    - client-logos.tsx contains `t('clients.title')`
    - resources/js/components/testimonial-card.tsx contains `export default function TestimonialCard`
    - testimonial-card.tsx contains `t('testimonials.` for translation key access
    - testimonial-card.tsx contains `Card` import from `@/components/ui/card`
    - resources/js/components/comparison-section.tsx contains `export default function ComparisonSection`
    - comparison-section.tsx contains `t('comparison.oldWay.title')` and `t('comparison.newWay.title')`
    - comparison-section.tsx contains both `X` or `Check` icon imports from lucide-react
    - resources/js/pages/public/home.tsx contains imports for: HeroSection, ServiceCard, ClientLogos, StatisticsSection, TestimonialCard, ComparisonSection, CtaSection
    - home.tsx contains `ScrollReveal` for section animations
    - home.tsx contains service card grid with 4 ServiceCard instances
    - home.tsx contains `PublicLayout` in the `.layout` property
    - home.tsx contains `Head` component with page title
    - home.tsx section order: HeroSection appears before ServiceCard grid which appears before ClientLogos which appears before StatisticsSection which appears before testimonials which appears before ComparisonSection which appears before CtaSection
    - npx tsc --noEmit produces no errors in these files
  </acceptance_criteria>
  <done>Complete landing page with all 7 sections assembled in correct order (D-15), all using ScrollReveal animations, bilingual translation keys, and responsive grid layouts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- home.tsx imports and renders all 7 section components
- All components use `t()` for text (no hardcoded English/Arabic strings)
- All sections use ScrollReveal or motion animation variants
- Service cards link to correct locale-prefixed service URLs
- Statistics counters use the custom useCounter hook with useInView
- Hero section has both dark: and light mode gradient classes
</verification>

<success_criteria>
- Landing page renders with all 7 sections in order: Hero, Service Cards, Client Logos, Statistics, Testimonials, Comparison, Bottom CTA
- Hero has animated gradient background with floating shapes and staggered text entrance
- 4 service cards link to their respective /{locale}/services/{slug} pages
- Statistics counters animate from 0 to target value on scroll
- Testimonials display in 3-card horizontal layout
- Comparison shows Old Way vs New Way in two columns
- All text from translation JSON, no hardcoded strings
- Animations respect prefers-reduced-motion
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-marketing-site/02-02-SUMMARY.md`
</output>
