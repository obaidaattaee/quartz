---
phase: 02-public-marketing-site
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - resources/js/components/process-steps.tsx
  - resources/js/pages/public/services/development.tsx
  - resources/js/pages/public/services/automation.tsx
  - resources/js/pages/public/services/qa.tsx
  - resources/js/pages/public/services/cybersecurity.tsx
  - resources/js/pages/public/about.tsx
  - resources/js/components/team-card.tsx
autonomous: true
requirements: [SRVC-01, SRVC-02, SRVC-03, SRVC-04, SRVC-05, TEAM-01, TEAM-02, TEAM-03]
user_setup: []

must_haves:
  truths:
    - "Each of the 4 service detail pages renders with hero, problem, approach, process steps, deliverables, and CTA"
    - "Service page CTA links to contact page with ?service={slug} query parameter"
    - "Process steps show numbered 1-2-3-4 flow with icons and stagger animation"
    - "About page shows company story, team member cards, and certifications section"
    - "Team cards display name, role, bio, and placeholder avatar"
    - "All content renders from translation keys in both EN and AR"
  artifacts:
    - path: "resources/js/components/process-steps.tsx"
      provides: "Numbered process step component with icon, used across all 4 service pages"
      contains: "export default function ProcessSteps"
    - path: "resources/js/pages/public/services/development.tsx"
      provides: "Software Development service detail page"
      contains: "export default function Development"
    - path: "resources/js/pages/public/services/automation.tsx"
      provides: "Automation service detail page"
      contains: "export default function Automation"
    - path: "resources/js/pages/public/services/qa.tsx"
      provides: "QA service detail page"
      contains: "export default function QA"
    - path: "resources/js/pages/public/services/cybersecurity.tsx"
      provides: "Cybersecurity service detail page"
      contains: "export default function Cybersecurity"
    - path: "resources/js/pages/public/about.tsx"
      provides: "About page with company story, team grid, certifications"
      contains: "export default function About"
    - path: "resources/js/components/team-card.tsx"
      provides: "Team member card with avatar, name, role, bio"
      contains: "export default function TeamCard"
  key_links:
    - from: "resources/js/pages/public/services/development.tsx"
      to: "/{locale}/contact?service=development"
      via: "Inertia Link with query param"
      pattern: "service=development"
    - from: "resources/js/pages/public/services/*.tsx"
      to: "process-steps.tsx"
      via: "import ProcessSteps"
      pattern: "import ProcessSteps"
    - from: "resources/js/pages/public/about.tsx"
      to: "team-card.tsx"
      via: "import TeamCard"
      pattern: "import TeamCard"
---

<objective>
Build all 4 service detail pages with shared process-steps component, and the About/Team page with team member cards and certifications.

Purpose: Service pages convert interest into action by showing problem-solution narratives and driving visitors to the contact form with pre-filled service type. The About page builds trust through team credentials and certifications.

Output: 4 service detail pages, 1 about page, and 2 shared components (ProcessSteps, TeamCard).
</objective>

<execution_context>
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eagle/Downloads/personal-project/Ahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-marketing-site/02-CONTEXT.md
@.planning/phases/02-public-marketing-site/02-RESEARCH.md
@.planning/phases/02-public-marketing-site/02-01-SUMMARY.md

<interfaces>
<!-- Phase 01 components and Phase 02 Plan 01 outputs -->

From resources/js/components/scroll-reveal.tsx:
```typescript
type Props = {
    children: ReactNode;
    delay?: number;
    className?: string;
    variant?: 'default' | 'hero' | 'stagger';
    as?: 'div' | 'section' | 'article' | 'ul';
};
export default function ScrollReveal(props: Props);
```

From resources/js/lib/animations.ts:
```typescript
export const fadeInUp: Variants;
export const fadeIn: Variants;
export const staggerContainer: Variants;
export const heroEntrance: Variants;
export const defaultTransition;
export const heroTransition;
```

From resources/js/hooks/use-locale.tsx:
```typescript
export function useLocale(): { locale: string; direction: string; isRTL: boolean; t: (key: string) => string; switchLocaleUrl: string };
```

From resources/js/layouts/public-layout.tsx:
```typescript
export default function PublicLayout({ children, breadcrumbs }: { children: ReactNode; breadcrumbs?: BreadcrumbItem[] });
// BreadcrumbItem = { title: string; href: string }
```

From resources/js/components/ui/button.tsx:
```typescript
// Button with variants: default, destructive, outline, secondary, ghost, link
// Sizes: default, sm, lg, icon
// Supports asChild prop for Link wrapping
```

From resources/js/components/ui/card.tsx:
```typescript
// Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
```

Translation key pattern for service pages (from en.json):
```
services.{slug}.hero.title
services.{slug}.hero.subtitle
services.{slug}.problem.title
services.{slug}.problem.description
services.{slug}.approach.title
services.{slug}.approach.description
services.{slug}.step1.title / .step1.description
services.{slug}.step2.title / .step2.description
services.{slug}.step3.title / .step3.description
services.{slug}.step4.title / .step4.description
services.{slug}.deliverables.title
services.{slug}.deliverables.0 through .4
services.{slug}.cta
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessSteps component and 4 service detail pages</name>
  <files>
    resources/js/components/process-steps.tsx,
    resources/js/pages/public/services/development.tsx,
    resources/js/pages/public/services/automation.tsx,
    resources/js/pages/public/services/qa.tsx,
    resources/js/pages/public/services/cybersecurity.tsx
  </files>
  <read_first>
    resources/js/components/scroll-reveal.tsx,
    resources/js/lib/animations.ts,
    resources/js/hooks/use-locale.tsx,
    resources/js/hooks/use-reduced-motion.tsx,
    resources/js/components/ui/card.tsx,
    resources/js/components/ui/button.tsx,
    resources/js/layouts/public-layout.tsx,
    resources/js/pages/public/home.tsx,
    resources/lang/en.json
  </read_first>
  <action>
    **1. Create `resources/js/components/process-steps.tsx`** (per D-05, SRVC-03):
    - Props: `{ serviceSlug: string; icons: LucideIcon[] }` where icons is an array of 4 Lucide icons
    - Renders a 4-step numbered process with ScrollReveal stagger animation
    - Layout: responsive grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
    - Each step: number badge (1-4, `w-10 h-10 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-bold text-lg`), Lucide icon (size-8 text-primary mt-3), title from `t('services.{serviceSlug}.step{n}.title')`, description from `t('services.{serviceSlug}.step{n}.description')`
    - Wrap the grid in `<ScrollReveal variant="stagger" as="div">` and each step in `<motion.div variants={fadeInUp}>` for stagger entrance
    - Connecting line between steps on desktop: use `hidden lg:block` absolute positioned line or border between items (optional visual enhancement)

    **2. Create a shared service page template pattern.** Each of the 4 service pages follows the identical structure, differing only in: `slug`, breadcrumb label, and the 4 icons passed to ProcessSteps. Create each page implementing this structure:

    **Service page structure (applied to all 4):**
    ```
    <Head title={t('services.{slug}.hero.title')} />

    {/* Hero banner */}
    <section className="relative bg-primary/5 dark:bg-primary/10 py-20 md:py-28">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal variant="hero">
          <ServiceIcon size={48} className="text-primary mb-4" />
          <h1 className="text-3xl md:text-5xl font-bold">{t('services.{slug}.hero.title')}</h1>
          <p className="text-lg text-muted-foreground mt-4 max-w-2xl">{t('services.{slug}.hero.subtitle')}</p>
        </ScrollReveal>
      </div>
    </section>

    {/* Problem section */}
    <section className="py-16 md:py-24">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2>{t('services.{slug}.problem.title')}</h2>
          <p>{t('services.{slug}.problem.description')}</p>
        </ScrollReveal>
      </div>
    </section>

    {/* Approach section */}
    <section className="py-16 md:py-24 bg-muted/30">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2>{t('services.{slug}.approach.title')}</h2>
          <p>{t('services.{slug}.approach.description')}</p>
        </ScrollReveal>
        <ProcessSteps serviceSlug="{slug}" icons={[Icon1, Icon2, Icon3, Icon4]} />
      </div>
    </section>

    {/* Deliverables section */}
    <section className="py-16 md:py-24">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2>{t('services.{slug}.deliverables.title')}</h2>
          <ul>
            {[0,1,2,3,4].map(i => <li key={i}><Check />{t('services.{slug}.deliverables.{i}')}</li>)}
          </ul>
        </ScrollReveal>
      </div>
    </section>

    {/* CTA section */}
    <section className="py-16 md:py-24 bg-primary/5 dark:bg-primary/10">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <ScrollReveal>
          <h2>{t('cta.title')}</h2>
          <Button size="lg" asChild>
            <Link href={`/${locale}/contact?service={slug}`}>{t('services.{slug}.cta')}</Link>
          </Button>
        </ScrollReveal>
      </div>
    </section>
    ```

    **3. Service-specific details:**

    **development.tsx:**
    - slug: `development`
    - Breadcrumbs: `[{ title: t('breadcrumb.home'), href: '/${locale}' }, { title: t('breadcrumb.services'), href: '#' }, { title: t('services.development.title'), href: '/${locale}/services/development' }]`
    - Hero icon: `Code2` from lucide-react
    - Process step icons: `Search` (Discovery), `Layers` (Architecture), `Code2` (Development), `Rocket` (Launch)
    - Layout: `Development.layout = (page) => <PublicLayout breadcrumbs={...}>{page}</PublicLayout>`

    **automation.tsx:**
    - slug: `automation`
    - Hero icon: `Zap` from lucide-react
    - Process step icons: `ClipboardCheck` (Audit), `PencilRuler` (Design), `Settings` (Implement), `TrendingUp` (Optimize)

    **qa.tsx:**
    - slug: `qa`
    - Hero icon: `ShieldCheck` from lucide-react
    - Process step icons: `Microscope` (Assessment), `Map` (Strategy), `Play` (Execution), `RefreshCw` (Continuous QA)

    **cybersecurity.tsx:**
    - slug: `cybersecurity`
    - Hero icon: `Lock` from lucide-react
    - Process step icons: `ScanSearch` (Assessment), `Shield` (Hardening), `Eye` (Monitoring), `HardDrive` (Resilience)

    Each page CTA links to `/${locale}/contact?service={slug}` per SRVC-04. This pre-fills the contact form service dropdown.
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && test -f resources/js/components/process-steps.tsx && test -f resources/js/pages/public/services/development.tsx && test -f resources/js/pages/public/services/automation.tsx && test -f resources/js/pages/public/services/qa.tsx && test -f resources/js/pages/public/services/cybersecurity.tsx && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "All 5 files exist"</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/components/process-steps.tsx contains `export default function ProcessSteps`
    - process-steps.tsx contains `ScrollReveal` with `variant="stagger"` or stagger animation pattern
    - process-steps.tsx reads translation keys `t('services.` with `.step1.title` through `.step4.title`
    - resources/js/pages/public/services/development.tsx contains `export default function Development`
    - development.tsx contains `service=development` in the CTA Link href
    - development.tsx contains `ProcessSteps` import
    - development.tsx contains `PublicLayout` in `.layout` property with breadcrumbs
    - development.tsx contains `t('services.development.hero.title')`
    - resources/js/pages/public/services/automation.tsx contains `export default function Automation`
    - automation.tsx contains `service=automation` in the CTA Link href
    - resources/js/pages/public/services/qa.tsx contains `export default function QA` or `QualityAssurance`
    - qa.tsx contains `service=qa` in the CTA Link href
    - resources/js/pages/public/services/cybersecurity.tsx contains `export default function Cybersecurity`
    - cybersecurity.tsx contains `service=cybersecurity` in the CTA Link href
    - All 4 service pages contain `Head` component import
    - All 4 service pages contain `ScrollReveal` for section animations (SRVC-05)
    - npx tsc --noEmit produces no errors for these files
  </acceptance_criteria>
  <done>4 service detail pages with hero, problem, approach, process steps (1-2-3-4 numbered flow with icons), deliverables, and per-service CTA linking to contact form with ?service= pre-fill. All text from translation keys, all sections animated with ScrollReveal.</done>
</task>

<task type="auto">
  <name>Task 2: About page with team cards and certifications</name>
  <files>
    resources/js/components/team-card.tsx,
    resources/js/pages/public/about.tsx
  </files>
  <read_first>
    resources/js/components/scroll-reveal.tsx,
    resources/js/hooks/use-locale.tsx,
    resources/js/components/ui/card.tsx,
    resources/js/components/ui/button.tsx,
    resources/js/layouts/public-layout.tsx,
    resources/js/pages/public/home.tsx,
    resources/lang/en.json
  </read_first>
  <action>
    **1. Create `resources/js/components/team-card.tsx`** (per D-21, TEAM-02):
    - Props: `{ index: number }` -- reads content from translation keys: `t('about.team.{index}.name')`, `t('about.team.{index}.role')`, `t('about.team.{index}.bio')`
    - Uses Card component from `@/components/ui/card`
    - Layout: placeholder avatar at top (a colored circle with initials derived from the name, e.g., first letter of first and last name, `w-20 h-20 rounded-full bg-primary/10 text-primary flex items-center justify-center text-xl font-bold mx-auto`), then name (font-semibold text-lg text-center), role (text-primary text-sm text-center), bio (text-muted-foreground text-sm mt-2)
    - Hover effect per D-21: on card hover, reveal additional info or subtle scale-up. Implement with `group` class on card and `group-hover:` transitions. Example: card gets `transition-all duration-300 hover:shadow-lg` and avatar gets `group-hover:scale-105`
    - Wrap in `<motion.div variants={fadeInUp}>` for stagger animation

    **2. Create `resources/js/pages/public/about.tsx`** (per D-20, TEAM-01, TEAM-02, TEAM-03):

    **Page structure:**
    ```
    <Head title={t('about.title')} />

    {/* Company story hero section */}
    <section className="relative bg-primary/5 dark:bg-primary/10 py-20 md:py-28">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal variant="hero">
          <h1 className="text-3xl md:text-5xl font-bold">{t('about.hero.title')}</h1>
          <p className="text-lg text-muted-foreground mt-4 max-w-3xl">{t('about.hero.description')}</p>
        </ScrollReveal>
      </div>
    </section>

    {/* Mission section */}
    <section className="py-16 md:py-24">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2 className="text-2xl md:text-3xl font-bold">{t('about.mission.title')}</h2>
          <p className="text-muted-foreground mt-4 max-w-3xl text-lg">{t('about.mission.description')}</p>
        </ScrollReveal>
      </div>
    </section>

    {/* Team section */}
    <section className="py-16 md:py-24 bg-muted/30">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2>{t('about.team.title')}</h2>
          <p>{t('about.team.subtitle')}</p>
        </ScrollReveal>
        <ScrollReveal variant="stagger" as="div" className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-12">
          <TeamCard index={0} />
          <TeamCard index={1} />
          <TeamCard index={2} />
          <TeamCard index={3} />
        </ScrollReveal>
      </div>
    </section>

    {/* Certifications section */}
    <section className="py-16 md:py-24">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ScrollReveal>
          <h2>{t('about.certifications.title')}</h2>
          <p>{t('about.certifications.subtitle')}</p>
        </ScrollReveal>
        <ScrollReveal variant="stagger" as="div" className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-6 mt-12">
          {[0,1,2,3,4,5].map(i => (
            <motion.div key={i} variants={fadeInUp} className="flex flex-col items-center gap-2 p-4 rounded-lg bg-muted/50">
              <Award className="size-8 text-primary" />  <!-- or Shield icon -->
              <span className="text-sm font-medium text-center">{t(`about.certifications.${i}`)}</span>
            </motion.div>
          ))}
        </ScrollReveal>
      </div>
    </section>
    ```

    - Breadcrumbs: `[{ title: t('breadcrumb.home'), href: '/${locale}' }, { title: t('breadcrumb.about'), href: '/${locale}/about' }]`
    - Layout: `About.layout = (page) => <PublicLayout breadcrumbs={...}>{page}</PublicLayout>`
    - Import `Award` (or `BadgeCheck`, `Shield`) from lucide-react for certification icons
    - Certification badges use same grayscale-to-color hover pattern as client logos (per D-22)
  </action>
  <verify>
    <automated>cd /Users/eagle/Downloads/personal-project/Ahmed && test -f resources/js/components/team-card.tsx && test -f resources/js/pages/public/about.tsx && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "Both files exist"</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/components/team-card.tsx contains `export default function TeamCard`
    - team-card.tsx contains `t('about.team.` for translation key access
    - team-card.tsx contains `Card` import from `@/components/ui/card`
    - team-card.tsx has avatar placeholder with initials (contains `rounded-full`)
    - resources/js/pages/public/about.tsx contains `export default function About`
    - about.tsx contains `t('about.hero.title')` and `t('about.hero.description')`
    - about.tsx contains `t('about.mission.title')`
    - about.tsx contains `t('about.team.title')` and `TeamCard` import
    - about.tsx renders 4 TeamCard instances (index 0 through 3)
    - about.tsx contains `t('about.certifications.title')`
    - about.tsx renders 6 certification items (indices 0 through 5)
    - about.tsx contains `PublicLayout` in `.layout` property with breadcrumbs
    - about.tsx contains `Head` component with `t('about.title')` as title
    - about.tsx contains `ScrollReveal` for section animations
    - npx tsc --noEmit produces no errors for these files
  </acceptance_criteria>
  <done>About page with company story hero, mission section, 4-person team grid with placeholder avatars, and 6 certification badges. All text bilingual via translation keys, all sections animated with ScrollReveal.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 4 service pages exist in resources/js/pages/public/services/
- Each service page has a CTA with ?service= query parameter
- ProcessSteps component renders 4 numbered steps with icons
- About page renders team cards and certification badges
- All components use t() for text content
- All sections use ScrollReveal for animations
</verification>

<success_criteria>
- 4 service detail pages each render hero, problem, approach, process steps, deliverables, and CTA
- Each service CTA links to /{locale}/contact?service={slug} (pre-fills contact form)
- Process steps show numbered 1-2-3-4 flow with icons and stagger animation
- About page shows company story, mission, team member grid (4 people), and certifications (6 badges)
- Team cards display placeholder avatar, name, role, and bio
- All content from translation JSON, no hardcoded strings
- Animations respect prefers-reduced-motion via ScrollReveal
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-marketing-site/02-03-SUMMARY.md`
</output>
